{
  "name": "project-kiwify",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page fictícia da Kiwify desenvolvida pela Verus Studio.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && python3 -m http.server 4173 --bind 127.0.0.1 --directory dist",
    "serve": "npm run dev",
    "check": "node --check script.js && npm run build"
  },
  "keywords": [
    "landing-page",
    "kiwify",
    "github-pages"
  ],
  "author": "Verus Studio",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "lenis": "1.3.25"
  }
}
